import styles from '@/components/LoadingSpinner/loading_spinner.module.css';

const LoadingSpinner = () => {
  return <span className={styles.loader}>Load&nbsp;ng</span>;
};

export default LoadingSpinner;
